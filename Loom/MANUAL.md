# Loom — User Manual (retired)

> **This document has been retired.** It described an early "v0.1 Phase-0" slice
> framed around transaction data and is no longer accurate (the tokenizer flow is
> now generic and domain-agnostic, and the `propose`/`embed`/`evaluate` verbs it
> said were unbuilt now exist).

For the current, authoritative documentation:

- **Tokenization (build a tokenizer for any foundation model):** [`TOKENIZATION.md`](./TOKENIZATION.md) — the user manual, with reproducible examples ([`examples/`](./examples/)) and a demo script.
- **Product + UX spec:** [`DESIGN.md`](./DESIGN.md)
- **How the harness is built:** [`ARCHITECTURE.md`](./ARCHITECTURE.md)
- **Running training on a GPU:** [`GPU-RUNBOOK.md`](./GPU-RUNBOOK.md)
- **Overview + the full verb list:** [`README.md`](./README.md)

The prior content remains in git history if you need it.
