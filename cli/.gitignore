# Node deps + build output (reproducible via `npm install && npm run build`)
node_modules/
dist/

# Pi runtime data + installed packages written into the branded agent home
home/sessions/
home/auth.json
home/.cache/
home/npm/
home/.loom-bootstrap.json
home/mcp-npx-cache.json

# misc
*.log
.DS_Store
